import { FilterShowListPipe } from './filter-show-list.pipe';

describe('FilterShowListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterShowListPipe();
    expect(pipe).toBeTruthy();
  });
});
